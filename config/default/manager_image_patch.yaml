apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
        - image: ghcr.io/jniedergang/cluster-api-provider-harvester:dev
          name: manager